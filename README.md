# dataframe-hash-table-implementation
 Custom DataFrame implementation in Java using hash tables with linear probing, dynamic resizing, and integrated data structures (BST, Linked Lists). Mimics Pandas functionality with O(1) average-case lookup.
